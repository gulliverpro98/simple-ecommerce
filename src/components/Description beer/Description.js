import styles from './Description.css';

const Description = ({id, description}) => {

    return(
        <div>
            {description}
        </div>
        
    )

}

export default Description;